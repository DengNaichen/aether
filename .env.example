# Aether Learning Platform - Environment Configuration Template
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

ENVIRONMENT="local"

# ---------------------------------
# SQL Database (PostgreSQL)
# ---------------------------------
# Database credentials (used by docker-compose.yml)
POSTGRES_USER="aether_user"
POSTGRES_PASSWORD="aether_password"
POSTGRES_DB="aether_db"

# For Docker development, use these values (db service in docker-compose.yml):
DATABASE_URL="postgresql+asyncpg://aether_user:aether_password@db:5432/aether_db"

# ---------------------------------
# Authentication & Security
# ---------------------------------
# IMPORTANT: Generate a secure SECRET_KEY for production!
# You can generate one with: openssl rand -hex 32
SECRET_KEY="your-secret-key-here-change-this-in-production"
ALGORITHM="HS256"
REFRESH_TOKEN_EXPIRE_DAYS=7
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ---------------------------------
# Neo4j Graph Database (Neo4j Aura)
# ---------------------------------
# TEAM: Get these credentials from your team lead
# These should point to the shared Neo4j Aura instance
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-aura-password
NEO4J_DATABASE=neo4j

# ---------------------------------
# Redis (Task Queue)
# ---------------------------------
# For Docker development, use the redis service:
REDIS_URL="redis://redis:6379/1"

# ---------------------------------
# Optional: OpenAI API (if using AI features)
# ---------------------------------
# OPENAI_API_KEY="your-openai-api-key"
