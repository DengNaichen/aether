# .env.docker
ENVIRONMENT="dev"

# ---------------------------------
# SQL Database & Authentication
# ---------------------------------
# 在 Docker 中使用服务名 'db' 而不是 'localhost'
DATABASE_URL="postgresql+asyncpg://aether_user:aether_password@db:5432/aether_db"
SECRET_KEY="b250f392765d8a9966df193da469a6907d3d5a81a77f857a8d19ea3765de2308"
ALGORITHM="HS256"
REFRESH_TOKEN_EXPIRE_DAYS=7
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ---------------------------------
# Neo4j Graph Database
# ---------------------------------
# 如果 Neo4j 在 Docker 中，请修改为服务名；如果在宿主机上，使用 host.docker.internal
NEO4J_URI=neo4j://host.docker.internal:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=d1997225
NEO4J_DATABASE=neo4j

# 在 Docker 中使用服务名 'redis' 而不是 'localhost'
REDIS_URL="redis://redis:6379/1"

# ---------------------------------
# Docker Compose 数据库配置（这些变量会被 docker-compose.yml 使用）
# ---------------------------------
POSTGRES_USER="aether_user"
POSTGRES_PASSWORD="aether_password"
POSTGRES_DB="aether_db"
